# 작업일지

---

2026.04.06 (월) - 프로젝트 초기 세팅 (기획 정리, 아키텍처 설계, 폴더 구조 생성, 개발 환경 구축, 구현 계획 수립)

2026.04.07 (화) - Phase 1 파이프라인 뼈대 구현 (BaseStage 추상 클래스, STAGE 매핑, Orchestrator, config/recipes yaml, main.py 진입점, src→aura 패키지명 변경, 더미 Stage로 동작 검증)

2026.04.14 (화) - Phase 2 FrameExtract Stage 구현 (2패스 블러 필터링, 시간 구간별 컷, 균등 다운샘플, 리사이즈+저장, Stage 인터페이스 __init__(config)+run(context)로 변경)

2026.04.15 (수) - Phase 2 Colmap Stage 구현 (subprocess로 외부 COLMAP CLI 호출, feature extraction/matching/mapper, bin 파일 검증, context 전달) + FrameExtract 버그 수정 (sorted glob, jpg_quality 반영, blur_chunk_min 로직 추가)

2026.04.16 (목) - Phase 2 GaussianSplatting Stage 초기 구조 작성 (pycolmap.Reconstruction으로 .bin 로드, KDTree 기반 scale 초기화, SH DC 색상 변환 (rgb/255-0.5)/0.28209, 가우시안 59채널 구성(xyz+scale+quat+alpha+f_dc+f_rest), CUDA 텐서 + requires_grad_ 설정, train 루프 뼈대)

2026.04.20 (월) - Phase 2 GaussianSplatting rasterization 구조 작성 (build_view/build_proj 메서드 분리, quaternion→rotation 변환, OpenGL 스타일 projection matrix 직접 구성, frustum culling을 view space에서 near/far + FOV 기반으로 구현, config에 near/far plane 추가, 이론.md gitignore 등록)

2026.04.24 (금) - Phase 2 GaussianSplatting Tile Rasterization 구현 (screen space 변환: clip→NDC→pixel, EWA splatting으로 3D 공분산을 2D로 projection(Jacobian·W·Σ·Wᵀ·Jᵀ + 0.3 blur), 고유값 기반 타원 반지름 계산, bounding box → tile grid 매핑, 타일별 (tile_num, depth) 64bit key 생성, q2rot·build_view를 batched torch 연산으로 변환, config에 tile_size 추가)
