# 계획

---

## Phase 1: 파이프라인 뼈대

1. src/stages/base.py
   - Stage 추상 클래스. 모든 Stage가 상속할 인터페이스(run 메서드) 정의

2. src/pipeline/stages.py
   - Stage 이름(문자열)을 실제 Stage 클래스로 매핑하는 등록소

3. src/pipeline/orchestrator.py
   - Recipe yaml을 읽고 Registry에서 Stage를 찾아 순서대로 실행하는 엔진

4. config/default.yaml
   - 데이터 경로, COLMAP 경로, 3DGS 학습 파라미터 등 설정값

5. recipes/recipes.yaml
   - 첫 번째 레시피. COLMAP + 원본 3DGS 조합의 Stage 실행 순서 정의

6. main.py
   - 진입점. recipe 이름과 입력 파일을 받아 Orchestrator 호출

## Phase 2: Stage 구현

7. src/stages/preprocess/frame_extract.py
   - 영상에서 프레임 추출 + 블러/저품질 프레임 필터링 (OpenCV)

8. src/stages/sfm/colmap.py
   - COLMAP CLI 호출. 특징점 추출 → 매칭 → 카메라 위치 + 포인트 클라우드 생성

9. src/stages/reconstruction/gaussian_splatting.py
   - 원본 3DGS 학습 실행. 포인트 클라우드에서 Gaussian 생성 → 반복 학습 → .ply 출력

10. src/stages/postprocess/ply_to_splat.py
    - .ply에서 Gaussian 파라미터를 읽어 .splat 바이너리 포맷으로 변환

## Phase 3: 통합

11. src/common/types.py
    - Stage 간 전달되는 데이터 구조 정의 (Phase 2 진행하며 구체화)

12. 통합 테스트
    - 테스트 영상으로 전체 파이프라인 실행. 영상 → .splat 끊김 없이 동작 확인

13. 에러 처리 / 로깅
    - Stage별 실패 시 에러 메시지 + 진행 상황 로그

---

## 미래 계획

### 아키텍처: Stage + Recipe 구조

파이프라인을 고정 단계로 묶지 않고, **Stage(처리 단위)를 Recipe(조합 설정)로 자유롭게 엮는 구조**를 채택한다.

#### 왜 이 구조인가

3DGS 분야는 모델 변형이 빠르게 나오고 있다.

- End-to-End 모델 (SfM 단계 자체가 없음)
- 새로운 전처리 추가 (동적 물체 제거 등)
- 학습 기반 초기화 (DUSt3R 등, COLMAP 대체)
- SfM + Trainer 통합 모델

고정 파이프라인으로는 모델이 나올 때마다 구조를 뜯어야 한다. Stage + Recipe 구조에서는 **Stage 하나 추가 + Recipe yaml 하나 작성**으로 대응 가능.

#### 적용 방법: PyYAML

- `pip install pyyaml`
- Recipe를 yaml 파일로 정의하면, Orchestrator가 yaml을 읽고 Registry에서 Stage 클래스를 찾아 순서대로 실행
- 사용하는 디자인 패턴: Pipeline Pattern + Strategy Pattern + Registry Pattern

---

### 인프라: Docker

#### 방침

- **프로토타입 단계에서는 Docker 없이 로컬 직접 실행**
- 클라우드 GPU 전환 시점에 Dockerfile 작성

#### 이유

1. 클라우드용 Docker는 CUDA 버전, 베이스 이미지, 볼륨 구조가 로컬용과 다름 → 지금 만들어도 나중에 다시 손봐야 함
2. 모델 디버깅 시 컨테이너 재빌드 반복은 비효율적
3. 기획 문서 방침과 동일: "Docker 적용 시점: 확장 단계에서 도입"

#### 지금 지켜야 할 것

**경로를 하드코딩하지 않는다.** 경로, 모델 위치, 임시 폴더 등을 전부 config로 분리해두면 Docker 전환 시 config 파일만 교체하면 된다.

```python
# X — Docker 전환 시 전부 수정
output_dir = "C:/Users/jaemi/Desktop/AURA/output"

# O — config만 바꾸면 됨
output_dir = config.output_dir
```

---

### 모델 최적화 방향

오픈소스 3DGS 기반으로 시작하여 커스텀 최적화하는 방식을 채택한다.

1. 원본 3DGS 오픈소스로 파이프라인 완성
2. 돌려보면서 문제점 파악 (속도, 품질, 메모리 등)
3. 다른 모델로 교체해서 비교 (Recipe 하나 추가)
4. 더 나은 쪽을 기반으로 커스텀 최적화 (학습 파라미터 튜닝, 전처리 개선, 도메인별 최적화 등)
